# Required
SSH_HOST=192.168.1.100
SSH_USER=myuser

# Auth — use one of these two:
SSH_PASSWORD=mysecretpassword
# SSH_KEY_PATH=C:/Users/<username>/.ssh/<keyid>

# Optional (defaults to 22)
# SSH_PORT=22
